apple = ''' This is a multi-line string.
It spans several lines.
It preserves whitespace and line breaks.
This is useful for documentation or large text blocks.'''

for character in apple :
    print(character)